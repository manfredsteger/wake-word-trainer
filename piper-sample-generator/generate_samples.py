# Stub — actual sample generation is handled by train.py using macOS TTS
def generate_samples(*args, **kwargs):
    raise RuntimeError("generate_samples stub called — this should not happen. Clips should be pre-generated.")
